# ✅ Let /api/embed/* go to Worker
/api/embed/*    404

# ✅ Other API routes can still fallback to static files if needed
/api/*          /api/:splat    200

# ✅ Catch-all SPA fallback for frontend
/*              /index.html    200
